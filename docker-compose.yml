version: '3.8'

services:
  kiro-go:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - CONFIG_PATH=/app/data/config.json
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GIST_ID=${GIST_ID}
    restart: unless-stopped
